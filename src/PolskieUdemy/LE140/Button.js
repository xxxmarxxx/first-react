import React, { PureComponent } from "react";

class Button extends PureComponent {
    state = {  }
    render() { 
        return ( 
            <button className='btn_mar'>switch user status</button>
         );
    }
}
 
export default Button;